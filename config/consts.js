const path = require('path');

module.exports = {
  SRC_DIR: path.resolve(__dirname, '..', 'src'),
  DIST_DIR: path.resolve(__dirname, '..', 'dist'),
  TYPINGS_DIR: 'typings',
  CHERRY_DIR: 'cherry',
};
